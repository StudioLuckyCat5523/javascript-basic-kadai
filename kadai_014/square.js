// JavaScript Document

//引数nemを2乗してその値を出力する
const userSquare = (number) => {
	//数値を二乗して戻り値を返す
	return number*number;
 
};

//関数を使う

const result = userSquare(10);
//結果

console.log(result);

